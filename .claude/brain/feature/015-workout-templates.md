---
route: /templates
entry_point: src/pages/Templates.tsx
category: Tracking
---

Saved reusable workout routines (exercise + sets + reps list); "Log entire session" logs every
item in one tap, defaulting each exercise's weight to its last logged set — noindexed.
